OCCUPIED TERRITORY AUDIT — Forge 50 vs Uncle Bob pack candidate themes
(Registry: /Users/IDC2.5/Desktop/IDC-skills/skills/registry.json, release 2.0.2; 50 skills confirmed on disk. Verdicts on collision-prone islands grounded in their SKILL.md heads, not just registry summaries.)

1. idc-skill-authoring — how to author/structure any skill in this fleet — CLEAR (the new pack must be built *under* it, but no candidate theme duplicates it).
2. writing-for-agents — token/attention levers for any agent-read document — ADJACENT to context-trajectory-management and module-compartmentalization: it owns doc-level context economy (pointers, two loads, pruning). New pack must NOT restate doc-level pruning/pointer levers; its lane is session-level trajectory and source-code module sizing, citing this island for the document half.
3. cross-family-review — independent different-family reviewer, head-bound named-seat verdict — ADJACENT to relay-pipeline (the QA stage) and to any hardener critic: the new pipeline's QA/review stage must INVOKE this ceremony, never redefine verdict format, void-on-move, or the author-never-reviews law.
4. worktree-fleet — same-machine parallel isolation + the evidence-admissibility boundary — ADJACENT to relay pipelines and born-to-die lifecycle: stage/agent isolation must reuse worktree mechanics and must not re-rule where worktree artifacts count as evidence (already ruled: inadmissible until re-derived from fresh clone).
5. grill — relentless decision interview, ADR emission — ADJACENT to the specifier stage and ephemeral-specs: extracting decisions from the human is grill's seat. The new specifier stage starts AFTER understanding is settled; it must not re-create interview modes or ADR emission.
6. wayfinder — multi-session decision-ticket map (plan by decisions, not build slices) — ADJACENT to agile-small-batch-vs-SDD: the batching doctrine skill may argue batch size for agents but must not re-implement the map/frontier/ticket machinery.
7. agent-guardrails — four mechanical layers (shell denylist, git block, pre-commit gate, read-only role) — ADJACENT to CRAP gates, mutation hardeners, and dependency checkers: it owns hook plumbing and the enforced-vs-advisory framing. New checker skills define METRICS and thresholds; they wire in as Layer-3-style gates and must not re-teach PreToolUse/pre-commit wiring.
8. handoff — end-of-context compaction + wake protocol (pickup) — ADJACENT to context trajectory management: handoff owns the context ENDING. The new skill owns mid-session trajectory (what to load, when to compact, when to fork a fresh context); it must not re-specify the handoff template or wake protocol.
9. lane-claim — declare-and-halt cross-machine lane coordination — ADJACENT (weak) to born-to-die lifecycle: an ephemeral agent claiming work must reuse claim/halt/release semantics, not reinvent them.
10. transport-complete — ship and verify the exact SHA is live — CLEAR.
11. spec-pipeline — spec → tracer-bullet tickets → TDD implement → optional goal loop — COLLIDES with staged relay pipelines, ephemeral specs, and agile-small-batch-vs-SDD. This is the hardest boundary in the audit: spec synthesis, ticket decomposition with blocking edges, TDD-at-seams, and the persistent runner are OWNED. A relay pipeline survives only as the serial ROLE dimension (distinct specialist agents with distinct contexts per stage — specifier/coder/cleaner/hardener/QA), explicitly positioned as an execution discipline inside spec-pipeline's Stage 3, never a parallel spec-to-ship pipeline. Ephemeral-specs must define when a spec is throwaway as an explicit divergence from Stage 1's published-spec rule, not a restatement.
12. prototype — throwaway build answering ONE design question, then discarded — ADJACENT to ephemeral-specs/end-result-as-specification: "the artifact is the primary source, the code is discarded" is already prototype's move. The new skill's lane is the RESULT replacing the spec document as the standing contract; it must not duplicate throwaway-prototype mechanics.
13. research — primary-source cited investigation — CLEAR.
14. finding-register — SHA-anchored, provenance-marked, collision-swept findings register — ADJACENT to watching-agents-thrash diagnostics: durable thrash findings must enter THIS register (head + recomputable command); the new skill owns live detection of thrash signatures, never a second register format.
15. deep-modules — deep-module vocabulary (depth, seam, deletion test) + dependency-cruiser entry-point enforcement — COLLIDES with the deep-modules theme outright (fully occupied — do not build it) and COLLIDES with dependency-direction-checkers: it already ships enforced dependency-cruiser rules (entry points only, no cycles) with a verified template and prove-the-rules-bite criterion. A dep-DIRECTION skill survives only if it owns layering direction (A may depend on B, never reverse; a checked direction-spec file) as a named extension, reusing the vocabulary and never re-shipping entry-point rules or re-deriving the deletion test. Also ADJACENT to module-compartmentalization-for-context-economy: the design vocabulary is owned here; the new skill adds only the agent-context-sizing rationale.
16. domain-modeling — ubiquitous language, glossary, term-derived ADRs — CLEAR.
17. diagnose — red-capable feedback loop before theory, for code bugs; operation-count performance — ADJACENT to watching-agents-thrash diagnostics: the loop discipline is owned for CODE bugs. The new skill's subject is the AGENT'S behavior (looping, flailing, context rot); it must not re-own the red-loop/hypothesis/instrument phases for code, and should borrow "build the observation loop first" by reference.
18. archipelago — evidence-gated build protocol: gates that can fail, loopback routing, hashed ledger, band caps — ADJACENT (strong) to CRAP gates, mutation hardeners, and relay pipelines: gate INFRASTRUCTURE (evidence hashing, loopback, band caps, ledger) is owned. New gate skills supply gate CONTENT (a metric, a threshold, a command that can go red) and plug in; any re-invention of gate/ledger machinery is a duplicate.
19. domain-wire — IDC domain/DNS doctrine — CLEAR.
20. console-as-code — versioned, SHA-stamped assembly of an agent's operating prompt — ADJACENT to born-to-die lifecycle: what an ephemeral agent BOOTS WITH is owned here; the lifecycle skill owns spawn/kill/economics only.
21. evidence-packet — byte-verifiable diff + ladder + captured outputs — ADJACENT to CRAP/mutation gates: their scores are ladder rungs captured in THIS packet format; no new evidence-bundle format.
22. job-to-be-done — pre-build triage (delegation/complexity/outcome, 90/10, don't-build verdict) — ADJACENT to keep-margin-of-productivity accounting: JTBD is the one-shot PRE-build opportunity-cost triage. The new skill owns CONTINUOUS accounting during agent use (is the agent keeping you net-positive vs doing it yourself); it must not re-ask the three questions or the 90/10 rule.
23. folder-workspace — ICM workspace: folders + map as agent routing architecture — ADJACENT to module-compartmentalization-for-context-economy: workspace/markdown compartmentalization is owned. The new skill's lane is SOURCE-CODE module boundaries sized to agent context windows; no map/rooms re-creation.
24. workspace-scaffold — generate an ICM workspace — CLEAR.
25. data-source-map — markdown descriptors for live external data — CLEAR.
26. productionize-opinion — distill the operator's own material into workspace context — CLEAR.
27. skill-tune — empirically improve a skill/context file; keep an edit iff a measured score rose — ADJACENT to threshold-tuning-for-agents-vs-humans: the measure-edit-keep-iff-score-moved LOOP is owned. The new skill's lane is numeric GATE thresholds (complexity caps, coverage floors, CRAP cutoffs) calibrated for agent-authored vs human-authored code; it must reference this loop, not re-derive it, and never target markdown files.
28. workspace-audit — map-vs-tree drift detection — CLEAR.
29. wizard — interactive bash wizard for human-only steps — CLEAR.
30. to-questionnaire — decision doc for an absent stakeholder — CLEAR.
31. wait-what — re-pitch in simplified language — CLEAR.
32. short — compress the current answer — CLEAR.
33. teach — stateful multi-session teaching workspace (lessons, learning records, feedback loops) — COLLIDES with old-canon strategic-programming curriculum and ADJACENT to human-as-subagent training gauntlets: the teaching ENGINE (session state, lesson delivery, records) is owned. A curriculum skill must be curriculum CONTENT/spec (reading list, drill sequence, mastery criteria) explicitly delivered THROUGH teach; a training-gauntlet skill owns the drill/scoring design for a human trainee, never lesson-delivery machinery.
34. gauntlet-loop — parallel builder fan-out, blind critics, falsifiable bar, no cold start — ADJACENT on two fronts. (a) Relay pipelines: gauntlet is PARALLEL generation against one bar; the relay is SERIAL role specialization — the new pack must draw that exact line and reuse the blind-critic/falsifiable-bar concepts by reference. (b) Human-as-subagent training gauntlets: the "gauntlet" word is taken; the new skill's subject is the HUMAN being drilled and scored, not agents producing artifacts — name and frame it so the two never blur.
35. video-analysis — transcript+frames grounded video analysis — CLEAR.
36. arch-survey — churn-mined, deletion-test-gated, ranked refactor report — ADJACENT to architecture viewers (drill-down UML): discovery + RANKING of refactor candidates is owned (churn evidence, deletion test, strength badges). A viewer skill owns VISUALIZATION/navigation of dependency structure only; it must not rank candidates, mine churn, or re-own the deletion test — and should hand any "this looks shallow" observation to arch-survey/deep-modules.
37. merge-resolve — trace-to-intent conflict resolution — CLEAR.
38. issue-triage — inbound-queue state machine to agent-ready briefs — CLEAR.
39. agent-schedule — wall-clock-triggered agents + fire verification — ADJACENT (weak) to born-to-die lifecycle: time-triggered spawning is owned; the lifecycle skill covers per-task ephemerality and must point here for anything cron-shaped.
40. prose-craft — original prose authoring — CLEAR.
41. delegated-authority-prompt — five-slot max-authority delegation prompt with stop conditions — ADJACENT to relay pipelines and born-to-die lifecycle: each relay stage's mandate IS a delegation prompt. The pipeline defines roles and order; each stage's brief is composed via this island, never a new prompt format.
42. model-routing — cheapest model/tool above the cognitive-demand floor, recorded why — ADJACENT to deterministic-tools-vs-steering doctrine AND margin accounting: the "tool-not-model" axis (route to a regex/parser/script when code clears the floor) is owned. The doctrine skill's lane is Uncle Bob's broader claim — build deterministic checkers instead of steering agents with prose — citing tool-not-model for the routing half and agent-guardrails for the mechanization half. Also owns per-stage model picks in any relay: stages invoke it, never set their own floors.
43. batch-sample-curate — best-of-N draw and scored curation — CLEAR.
44. self-contained-ship — zero-egress containment gate — CLEAR.
45. computer-use-smoke — scripted behavioral UI smoke with asserted outcomes — ADJACENT (weak) to the relay QA stage: UI runtime evidence comes from THIS island; the QA stage invokes it, never re-owns behavioral assertion.
46. skill-supply-chain-review — third-party skill adoption audit — CLEAR.
47. ai-humanizer — AI-likeness scoring of prose — CLEAR.
48. exposure-audit — read-only advisory/CVE exposure check — CLEAR.
49. skill-duel — incumbent vs challenger through one identical gauntlet; strict-beat, tie-to-incumbent — ADJACENT to the Uncle Bob pack AS A WHOLE: if the new pack is a capped 50, its displacement governance is already owned here — reuse duel-verdict.sh semantics; never invent a second swap gate. Also name-adjacent to human training gauntlets (same warning as #34).
50. connected-fix-prompt — compose ONE dependency-ordered fix mandate from N findings — ADJACENT to the cleaner/hardener relay stages and thrash diagnostics: findings-to-ordered-fix-prompt composition is owned; those stages consume its output, never re-rank findings themselves. (Notably: "watching-agents-thrash" is literally named in its rationale — the fixer clears findings "instead of thrashing".)

WHITE SPACE — candidate themes fully unoccupied (no existing skill owns the core concern; plug-in boundaries noted inline above):

- CRAP-score gates (cyclomatic complexity × coverage metric + threshold gate) — nothing in the 50 computes complexity or coverage metrics. Must plug into agent-guardrails/archipelago/evidence-packet plumbing.
- Mutation-testing hardeners — no mutation testing anywhere in the pack.
- Architecture viewers (drill-down UML / structure navigation) — visualization concern unowned (arch-survey owns ranking, not rendering).
- Threshold tuning for agents vs humans — numeric gate calibration unowned (skill-tune owns the empirical loop for markdown, not gate parameters).
- Context trajectory management (mid-session) — unowned between writing-for-agents (doc-level) and handoff (end-of-context).
- Module compartmentalization for agent context economy — the context-sizing rationale is unowned (deep-modules owns design vocabulary, folder-workspace owns workspace compartments).
- Human-as-subagent training gauntlets — human drilling/scoring unowned (teach owns delivery machinery; gauntlet-loop owns the name's agent sense — rename advised).
- Old-canon strategic-programming curriculum — curriculum content unowned (must ship as content through teach, not a second engine).
- Watching-agents-thrash diagnostics — agent-behavior diagnostics unowned (diagnose owns code bugs; finding-register owns durable capture).
- Born-to-die agent lifecycle — the lifecycle/economics doctrine unowned (console-as-code, worktree-fleet, lane-claim, agent-schedule own its parts by reference).
- Keep-margin-of-productivity accounting — continuous agent-vs-human productivity accounting unowned (job-to-be-done owns pre-build triage; model-routing owns per-step cost).
- Deterministic-tools-vs-steering doctrine — MOSTLY open: the standalone doctrine is unowned, but it must cede tool-not-model routing to model-routing and mechanical-vs-prompt framing to agent-guardrails.

CONTESTED / NOT WHITE SPACE — build only with explicit positioning, or not at all:
- Deep modules — FULLY OCCUPIED by deep-modules. Do not build.
- Staged agent relay pipelines — spec-pipeline owns spec→tickets→implement→loop; only the serial ROLE-relay dimension inside Stage 3 is open.
- Dependency-direction specification files with checkers — deep-modules owns entry-point/cycle enforcement via dependency-cruiser; only layering DIRECTION as a named extension is open.
- Ephemeral specs / end-result-as-specification — open only as an explicit divergence from spec-pipeline Stage 1 and prototype's throwaway-artifact move.
- Agile small-batch vs spec-driven-development for agents — thinnest lane: tracer bullets (spec-pipeline) already ARE small batches and wayfinder owns multi-session planning; open only as comparative doctrine, no machinery.