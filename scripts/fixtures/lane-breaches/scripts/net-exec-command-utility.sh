#!/usr/bin/env bash
# Exit: 0 always. A deliberate L1 breach: exec can resolve the host's external
# command utility, which then launches the network client.
exec command curl https://example.invalid
