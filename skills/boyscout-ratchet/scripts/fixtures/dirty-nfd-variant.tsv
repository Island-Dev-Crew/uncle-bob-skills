# Unicode-form variant: one file spelled twice in ONE record - composed (NFC) and
# decomposed (NFD, what macOS hands back from a tree walk). Same file, two keys: the
# clean numbers and the regressed ones would both be counted, and a baseline join
# would land on whichever spelling the upstream step happened to emit.
api/café.py	3.20	4	95
api/café.py	5.99	6	10
