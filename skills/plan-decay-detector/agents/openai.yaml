# model-invoked skill — interface metadata only
interface:
  display_name: "Plan Decay Detector"
  short_description: "Mid-execution divergence check - the plan states its assumptions in checkable form, and a gate reports the moment the repo stops matching them, exiting 1 to call for a re-plan. It rings the bell; no hook here stops a fleet"
