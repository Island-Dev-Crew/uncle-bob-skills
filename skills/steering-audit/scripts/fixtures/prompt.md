# Known fixture — a three-rule standing prompt

Deliberately tiny; the list order below is load-bearing (extract emits R1-R3).

- functions under 50 lines
- prefer editorial layouts over template grids
- no hardcoded secrets
