# Steering rules (non-markdown markers)

Ten bullet glyphs, ten en-dashes, ten hyphens split by a zero-width space, ten R-labelled ordinals.

• keep every module under two hundred lines.
• name booleans with an is or has prefix.
• run the formatter before every commit.
• put integration tests in tests/integration.
• log at info for lifecycle events only.
• pin every dependency to an exact version.
• wrap external calls in a timeout.
• return typed errors from the service layer.
• keep migrations forward-only.
• document any public function over ten lines.
– keep every module under two hundred lines.
– name booleans with an is or has prefix.
– run the formatter before every commit.
– put integration tests in tests/integration.
– log at info for lifecycle events only.
– pin every dependency to an exact version.
– wrap external calls in a timeout.
– return typed errors from the service layer.
– keep migrations forward-only.
– document any public function over ten lines.
-​ keep every module under two hundred lines.
-​ name booleans with an is or has prefix.
-​ run the formatter before every commit.
-​ put integration tests in tests/integration.
-​ log at info for lifecycle events only.
-​ pin every dependency to an exact version.
-​ wrap external calls in a timeout.
-​ return typed errors from the service layer.
-​ keep migrations forward-only.
-​ document any public function over ten lines.
R1. keep every module under two hundred lines.
R2. name booleans with an is or has prefix.
R3. run the formatter before every commit.
R4. put integration tests in tests/integration.
R5. log at info for lifecycle events only.
R6. pin every dependency to an exact version.
R7. wrap external calls in a timeout.
R8. return typed errors from the service layer.
R9. keep migrations forward-only.
R10. document any public function over ten lines.
