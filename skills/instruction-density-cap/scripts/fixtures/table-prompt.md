# Rules, as a table

Some teams keep their steering rules in a table rather than a bullet list. One row
per rule is still one directive per rule.

| id | rule | destination |
|----|:-----|-------------|
| R1 | keep functions short | prompt |
| R2 | kebab-case file names | prompt |
| R3 | one logical change per commit | prompt |
| R4 | secrets come from the environment | prompt |
| R5 | rate-limit each public endpoint | prompt |
