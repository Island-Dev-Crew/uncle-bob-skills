# Known-MALFORMED: two CommonMark H1 blocks make two reader-visible rulings for M1.

# M1
  mutation:   `+` changed to `-`
  argument:   the right operand is always zero, so both operators compute the same observable value.
  excused-by: First Reviewer
  head:       4f2c1ab9d3e7c0b58a61d2f4e9c73b0a1d6e5f28

# M1
  mutation:   `+` changed to `*`
  argument:   the right operand is always one, so both operators compute the same observable value.
  excused-by: Second Reviewer
  head:       9a3c2e7b4d1f6085a76c9b0e3f2d1a4c5b6e7f80
