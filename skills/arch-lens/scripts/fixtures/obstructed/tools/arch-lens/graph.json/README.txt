this file exists so git can carry the DIRECTORY above it. that directory is the fixture:
something already occupies the name graph.json, and it is not a lens file the renderer can
replace - creating graph.json here means removing a subtree nobody described.
