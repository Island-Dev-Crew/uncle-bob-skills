# BOM copy of clean-diff-scoped.tsv: the same seven declarations behind a leading U+FEFF.
# A byte-order mark is an editor's file-format artifact, so it is stripped and the word it
# prefixes is judged as the word it is — exit 0, never a bad row.
# language<TAB>gate<TAB>tool<TAB>scope_arg<TAB>command

java	crap	crap4java	--changed	crap4java --changed --threshold 6
typescript	mutation	stryker	--incremental	./node_modules/.bin/stryker run --incremental
java	mutation	pit	-DwithHistory	mvn org.pitest:pitest-maven:mutationCoverage -DwithHistory=true
rust	mutation	cargo-mutants	--in-diff	cargo mutants --in-diff story.diff
python	mutation	mutmut	--incremental	mutmut run --incremental --msg 'note # 1'
typescript	mutation	stryker	--incremental=true	./node_modules/.bin/stryker run --incremental=true
rust	mutation	cargo-mutants	--in-diff=café.diff	cargo mutants --in-diff=café.diff
