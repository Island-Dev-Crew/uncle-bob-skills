# model-invoked skill — interface metadata only
interface:
  display_name: "Mutant Hunt"
  short_description: "The merciless hardener's metric - zero surviving non-equivalent mutants on the story diff, covered lines only, under a runtime budget cap; diff scoping is enforced here, the mutation run itself is advisory at v0 and gated by your language's own tool"
